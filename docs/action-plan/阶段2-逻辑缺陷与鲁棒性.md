# 阶段 2 —— 逻辑缺陷与鲁棒性(P1)

> 目标:消灭会导致"必死局 / 误操作 / 静默死锁 / 崩溃"的逻辑缺陷。
> 完成标准:本阶段 5 个任务全部 DoD,无静默失败路径。

---

## T2.1 混沌判负完整 + 水元素反制(TC-07)

- 优先级: P1 · 预估: 3-4h · 状态: [x] 已完成 · 依赖: 无
- 现状: `chaos_check()` 只发 `game_over` 信号、`game_ended` 不置位;水元素无灭绝牌,水 >50% 即必死。
- 改动点:
  1. `src/main/GameManager.gd` `chaos_check()`:`game_over.emit` 前后补 `game_ended = true`、`phase = Phase.EVOLVE`(与死寂路径一致)。
  2. 平衡方案(二选一,建议 A):
     - A: 新牌「干涸」EXTINCTION(水 ≥5 → 清空水,也清草),`data/rules.json` 追加 `level=0`;
     - B: 水生成 2/回合 → 1/回合。
  3. `CHAOS_ELEMENTS` 从硬编码改为关卡 JSON 字段 `chaos_elements`(默认沿用现值)。
- 验收: 更新 TC-07(`game_ended == true`);新牌配 `run_tests.gd` 用例;重跑 TC-06 确认难度未回归。
- DoD: 任意元素失控均有理论反制手段;README 规则表同步新牌。
- 实际落地: `rules.json` 新增「干涸」(WATER EXTINCTION, 也清 GRASS);`GameManager` 补 `game_ended/phase`、`chaos_elements` 改读关卡 JSON(默认沿用现值);`ChainReaction` 尘播撒改走可播种 `GameManager.rng`;TC-06 因牌池变化重标定种子 20240816 并通过。

## T2.2 演化后清空选中(TC-28b)

- 优先级: P1 · 预估: 0.5h · 状态: [x] 已完成 · 依赖: 无
- 现状: 演化后 `selected_card_idx` 残留,误点网格会按旧索引误打手牌并耗能量。
- 改动点: `scenes/Main.gd` `_on_execute()`:`grid_renderer.select_card(-1)`;`GameManager.execute()` 入口兜底校验手牌索引有效性。
- 验收: 更新 TC-28b:`selected_card_idx == -1`。
- DoD: 演化结束点网格不会误打牌。
- 实际落地: `scenes/Main.gd` 三个演化入口(执行/4x/跳过)先 `grid_renderer.select_card(-1)`;`play_card()` 原有索引校验保留为兜底。

## T2.3 空/损坏 rules.json 降级(TC-23b)

- 优先级: P1 · 预估: 2h · 状态: [x] 已完成 · 依赖: 无
- 现状: `_load_rules()` 失败 → 空数组 → 手牌 0、`can_play_card()` 恒 false,玩家静默死锁。
- 改动点:
  1. `src/main/GameManager.gd`:失败时 `push_error` + `game_over(false, "规则数据损坏…")`。
  2. 内置 `DEFAULT_RULES`(steamify/grow/extinct 最小牌组)作降级后备,保证可玩。
  3. 关卡 JSON 同理:尺寸钳制 6-32、target 钳制 1-99999、字段缺失给默认值。
- 验收: 更新 TC-23b:空文件 → 降级牌组 3 张可玩;损坏文件 → 明确失败提示;恢复文件后正常。
- DoD: 无静默死锁路径;README 已知问题删除对应条目。
- 实际落地: `_load_rules` 失败 push_error + game_over + `rules_data_error`;合法空数组走内置 DEFAULT_RULES(steamify/grow/extinct);`_load_level` 尺寸钳制 6-32、target 钳制 1-99999、非法元素条目跳过;TC-23b 增加关卡字段容错断言。

## T2.4 RuleCard.from_dict 非法字段校验(TC-25)

- 优先级: P1 · 预估: 1h · 状态: [ ] 待开始 · 依赖: 无
- 现状: `Kind.get("XXX")` 返回 null 赋 int 抛错,后续字段全部丢失。
- 改动点: `src/rules/RuleCard.gd` `from_dict()`:kind 先在 `Kind` 中查 key,不含 → 整条跳过 + `push_warning`;radius/life/threshold 用 `clampi` 钳制。
- 验收: 更新 TC-25:非法 kind 整条跳过不崩溃,合法字段不受影响;`run_tests.gd` 补一条。
- DoD: 任意非法 JSON 不引发运行时错误。

## T2.5 演化中切场景的协程竞态(TC-30c)

- 优先级: P1 · 预估: 1.5h · 状态: [ ] 待开始 · 依赖: 无
- 现状: `execute()` 协程挂在 GameManager 上,切场景不中断;若此时 `start_game()` 重开,旧协程继续写新网格。
- 改动点:
  1. `src/main/GameManager.gd`:保存 `_running` 协程引用,`start_game()` 先 cancel;`execute()` 设/清标志。
  2. `src/rules/ChainReaction.gd`:加 `cancelled` 标志,每轮循环检查,被取消即返回已计连锁。
- 验收: 扩展 TC-30c:切场景后旧演化中止,新局 `chain_total == 0` 不被污染。
- DoD: 连续快速切场景 + 重开无残留状态。
