# 阶段 1 —— 稳定性修复(P0)

> 目标:消灭直接破坏体验的四大问题(手牌溢出 / 演化干等 / 进度丢失 / 贴图缩放失真)。
> 完成标准:本阶段 5 个任务全部 DoD,白盒测试 headless 全绿。

---

## T1.1 提交当前工作区修复

- 优先级: P0 · 预估: 0.5h · 状态: **[x] 已完成(本次提交)**
- 内容: `scenes/Main.tscn` 帮助文本/HintLabel/MenuButton/NextButton 乱码修复(TC-04/TC-05);`src/ui/GridRenderer.gd` 图例补全孢/冰(TC-19)。
- 验收: TC-04 / TC-05 / TC-19 转正为"正常"通过;`白盒测试用例.md` 中移除对应"已知缺陷"标注。

## T1.2 手牌上限 + 布局溢出修复(TC-14 / TC-20)

- 优先级: P0 · 预估: 2-3h · 状态: [ ] 待开始 · 依赖: 无
- 现状: `HandManager.draw()` 无限抽牌,30 回合 = 95 张;`HandContainer` 仅 1100px 宽,单卡 150px,第 4 关 13 张牌池必然溢出。
- 改动点:
  1. `src/player/HandManager.gd`:加 `max_hand = 8`,`draw()` 超限时弃牌(维护弃牌堆);`fill_draw_pile()` 支持弃牌堆循环重洗。
  2. `scenes/Main.gd` `_refresh()`:卡宽 150 → 100,或 `HandContainer` 换 `GridContainer` / 包 `ScrollContainer`。
  3. `Main.tscn`:手牌区加计数标签("手牌 5/8")。
- 验收: 更新 TC-14(30 回合后 `hand_size() <= 8`)、TC-20(13 张卡池不溢出);新增"满手牌抽牌→弃牌堆"单测。
- DoD: headless 全绿;README 已知问题删除"手牌无上限"条。

## T1.3 演化跳过 / 加速按钮(TC-15)

- 优先级: P0 · 预估: 2h · 状态: [ ] 待开始 · 依赖: 无
- 现状: `execute_async` 每次 Reaction 0.1s + 1 帧,200 连锁实机 18-45 秒,无法跳过。
- 改动点:
  1. `src/rules/ChainReaction.gd`:`execute_async(grid, pillars, frame_delay, speed)`;`speed=∞` 时改走同步 `execute()`。
  2. `src/main/GameManager.gd`:`execute(speed)` 透传;演化阶段置 `phase=EVOLVE` 期间 UI 可调速度。
  3. `scenes/Main.gd` + `Main.tscn`:"执行演化"旁加"加速"与"跳过"按钮(仅演化阶段可见);跳过后批量刷新(合并 `flash_cell` 风暴为一次 `state_changed`)。
- 验收: TC-15 新增断言:同棋盘 speed=∞ 时 200 连锁 <2s;1x/4x 结果一致(连锁数与终局网格哈希相同)。
- DoD: 三档速度可切换;README 已知问题删除"演化无跳过"条。

## T1.4 通关进度持久化(TC-13)

- 优先级: P0 · 预估: 2h · 状态: [ ] 待开始 · 依赖: 无
- 现状: `LevelManager.unlocked` 仅存内存,重启回到第 1 关。
- 改动点:
  1. `src/world/LevelManager.gd`:构造时 `ConfigFile` 读 `user://save.cfg`;`advance()` 写回 `unlocked`。
  2. 关卡选择页(`src/ui/LevelSelect.gd` / `LevelSelect.tscn`)加"重置进度"按钮(测试用)。
  3. 预留字段:`{unlocked, last_level}` 供后续完整存盘(T3.6 撤销/快照)复用。
- 验收: 更新 TC-13:新实例 `unlocked` 保留;删除存档后回退 0;补"写盘失败不崩溃"用例。
- DoD: 通关 → 重启 → 关卡仍解锁;README 已知问题删除"进度不持久化"条。

## T1.5 全量贴图缩放审计与修正(新增)

- 优先级: P0 · 预估: 4h · 状态: [ ] 待开始 · 依赖: 无
- 现状(已实测 `assets/` 全部 30 张 PNG 原生尺寸):
  1. 元素/状态/柱/尘贴图原生 **1024×1024**(earth/ice/overlay_frozen 为 1254×1254),绘制到 26-64px(约 1/16-1/20 缩小),且导入为默认线性过滤 → 缩小后发糊、闪烁;**TC-22 现有断言的前提("原图 64px")与真实原生尺寸不符,该用例需按本任务重写**。
  2. `Main.tscn` 面板背景拉伸变形:help_bg(1536×1024, 1.5:1)→ HelpPanel 700×650(1.08:1);gameover_bg(1836×856, 2.15:1)→ GameOverPanel 700×150(4.67:1),变形严重;ui_bg(1538×1022, ≈1.5:1)→ 1500×1000 轻微;level_select_bg(1536×1024, 1.5:1)→ 1500×1000 比例匹配,正常。
  3. 疑似未使用资产:btn_execute/help/level/menu/next/retry(场景按钮均为纯文本 Button)、card_frame(RuleCardView 为代码 Button)、wind_icon(风当前用文字显示)。
- 改动点:
  1. 项目设置 `rendering/textures/canvas_textures/default_texture_filter` → **Nearest**(或 GridRenderer 节点设 `texture_filter`),保证元素/状态贴图在 52-64px 下清晰锐利。
  2. `Main.tscn`:HelpPanel/GameOverPanel 的 rect 调整为与贴图同宽高比(或改用保持比例的 stretch 方案),做到不变形、不越出视口。
  3. 建立贴图台账(原生尺寸 → 绘制尺寸 → 缩放比例 → 是否变形/溢出),与设计意图不符的列入替换清单(建议元素源图重出 64px 像素版,而非 1024 强行缩小)。
  4. 未使用资产确认后删除,或移交 T4.4 清理;card_frame 若在 T3.5 卡面资源化中留用,需按卡面实际尺寸重切。
- 验收:
  1. 重写 TC-22:断言改为"绘制尺寸/真实原生尺寸比例在容差带内或为整数倍"。
  2. 新增白盒用例 TC-22b:枚举场景内所有 TextureRect,断言 rect 宽高比与贴图宽高比偏差 <5%,且 rect 不越出 1500×1000 视口。
  3. 人工验收:4 关网格、帮助面板、结算面板、关卡选择页——无溢出、无变形、无过小/过糊。
- DoD: 全部画面贴图无溢出、无变形、无不合理缩放;TC-22/TC-22b 通过;README 已知问题补记"贴图缩放"修复结论。
