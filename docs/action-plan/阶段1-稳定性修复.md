# 阶段 1 —— 稳定性修复(P0)

> 目标:消灭直接破坏体验的四大问题(手牌溢出 / 演化干等 / 进度丢失 / 贴图缩放失真)。
> 完成标准:本阶段 5 个任务全部 DoD,白盒测试 headless 全绿。
> **状态:全部完成(2026-08)· 验证:核心自检 30/30 ALL PASS,白盒 36/36 全绿,主场景冒烟无报错。**

---

## T1.1 提交当前工作区修复

- 优先级: P0 · 预估: 0.5h · 状态: **[x] 已完成(本次提交)**
- 内容: `scenes/Main.tscn` 帮助文本/HintLabel/MenuButton/NextButton 乱码修复(TC-04/TC-05);`src/ui/GridRenderer.gd` 图例补全孢/冰(TC-19)。
- 验收: TC-04 / TC-05 / TC-19 转正为"正常"通过;`白盒测试用例.md` 中移除对应"已知缺陷"标注。

## T1.2 手牌上限 + 布局溢出修复(TC-14 / TC-20)

- 优先级: P0 · 预估: 2-3h · 状态: **[x] 已完成**
- 现状: `HandManager.draw()` 无限抽牌,30 回合 = 95 张;`HandContainer` 仅 1100px 宽,单卡 150px,第 4 关 13 张牌池必然溢出。
- 改动点(实际落地):
  1. `src/player/HandManager.gd`:`MAX_HAND = 8`,超限抽牌进弃牌堆,抽牌堆耗尽时弃牌堆循环重洗。
  2. `scenes/Main.gd`:卡面 150×90 → **130×80** + `RuleCardView` 字号 12;`Main.tscn` 加"手牌 5/8"计数标签。
  3. `tests/run_tests.gd` 新增 `_test_hand_cap`(封顶/守恒/循环重洗)。
- 附带发现与处理:白盒 TC-06 贪心机器人依赖旧"无上限手牌"行为,上限落地后暴露索引越界与手牌滞留问题 → 重写贪心为"优先级引擎"(熔岩/矿/汽有限资源优先,岩化→草生→草殖→灭绝可持续循环);并引入 `GameManager.rng`(`RandomNumberGenerator`,Godot 4.7 的 `randi()` 流不可播种)替代全部游戏逻辑随机,TC-06/TC-10 固定种子后轨迹完全确定(为 T3.8 种子化打基础)。
- 验收: 更新 TC-14(30 回合后 `hand_size() == 8`,牌守恒)、TC-20(8 张 × 130px = 1082 ≤ 1100);TC-06 定种后确定性通过。
- DoD: headless 全绿;README 已知问题删除"手牌无上限"条。

## T1.3 演化跳过 / 加速按钮(TC-15)

- 优先级: P0 · 预估: 2h · 状态: **[x] 已完成**
- 现状: `execute_async` 每次 Reaction 0.1s + 1 帧,200 连锁实机 18-45 秒,无法跳过。
- 改动点(实际落地):
  1. `src/rules/ChainReaction.gd`:`execute_async(grid, pillars, frame_delay, speed)`,`speed<=0` 改走同步 `execute()`;否则延时除以 speed。
  2. `src/main/GameManager.gd`:`execute(speed)` 透传。
  3. `scenes/Main.gd` + `Main.tscn`:"4x加速"与"跳过"按钮(仅演化阶段可见)。
- 验收: TC-15 三档断言:1x 耗时 (18s,45s];跳过档 <2s 且连锁/终局网格与同步执行一致(同种子);4x <12s 且快于 1x、结果一致。
- DoD: 三档速度可切换;README 已知问题删除"演化无跳过"条。

## T1.4 存档系统:三槽位 + 命名 + 覆盖确认 + 秒级时间戳(TC-13 系列)

- 优先级: P0 · 预估: 8h · 状态: **[x] 已完成**
- 需求(用户补充规格,替代原"仅解锁进度持久化"方案):
  1. **三个存档槽**:存档分两层——meta 区存全局通关进度(`unlocked`,删档不丢),槽位区存单局快照(网格/柱/手牌/能量/回合/连锁/风/关卡)。
  2. **任意一次演化结束后可存档**:布局阶段随时可点"存档" → 选择槽位(3 选 1)+ 输入存档名。
  3. **覆盖规则**:目标槽非空且**不是本局加载来源槽**时弹确认框;`loaded_from_slot` 标记**仅在加载时置位**(普通存档不改标记,符合"从该存档上加载游戏的就不再提示"的规格);删除来源槽后标记置 -1。
  4. **开始游戏必须选档或开新游戏**:新增启动入口 `scenes/SaveSelect.tscn`(`project.godot` main_scene 改指),3 槽列表(名称+时间,空槽显示"空槽位")+「新游戏」+「调整存档」。
  5. **主界面"调整存档"按钮**:重命名 / 删除(需确认)。
  6. **时间显示精确到秒**:`Time.get_datetime_string_from_unix_time(t, true)`(Godot 4.7 该函数仅 2 参)→ `YYYY-MM-DD HH:MM:SS`。
- 改动点(实际落地):
  1. 新建 `src/world/SaveManager.gd`(槽位读写/全局 unlocked/`use_path()` 测试隔离/序列化快照)。
  2. `src/main/GameManager.gd`:`save_game/load_game/_snapshot_game/_restore_snapshot/ensure_game_started/persist_unlock`;`_ready` 不再自动开局,应用持久化 unlocked。
  3. 新场景 `scenes/SaveSelect.tscn` + `src/ui/SaveSelect.gd`;`scenes/Main.gd` "存档"按钮 + 弹窗(选槽+命名+覆盖确认);通关时 `persist_unlock()`。
- 验收: TC-13(存读一致、unlocked 独立持久化)、TC-13b(覆盖确认/来源槽静默/删除标记)、TC-13c(秒级时间戳、重命名/删除)、TC-13d(序列化 round-trip)全部通过;TC-01 扩展 SaveSelect 冒烟。
- DoD: 选档 / 新游戏 / 调整存档三流程可走通;README 已更新存档说明与已知问题。

## T1.5 全量贴图缩放审计与修正(新增)

- 优先级: P0 · 预估: 4h · 状态: **[x] 已完成**
- 现状(已实测 `assets/` 全部 30 张 PNG 原生尺寸):
  1. 元素/状态/柱/尘贴图原生 **1024×1024**(earth/ice/overlay_frozen 为 1254×1254),绘制到 26-64px(约 1/16-1/20 缩小),且导入为默认线性过滤 → 缩小后发糊、闪烁。
  2. `Main.tscn` 面板背景拉伸变形:help_bg(1.5:1)→ HelpPanel 700×650(1.08:1);gameover_bg(2.15:1)→ GameOverPanel 700×150(4.67:1),变形严重。
- 改动点(实际落地):
  1. `project.godot`:`rendering/textures/canvas_textures/default_texture_filter=0`(Nearest)。
  2. `Main.tscn`:HelpPanel 700×650 → **975×650**(1.5:1),HelpLabel 加宽至 943;GameOverPanel 700×150 → **700×326**(2.147:1)。
  3. 新建 `docs/贴图台账.md`(全部资产原生/绘制/比例/状态)。
  4. 删除未引用资产 btn_×6 + wind_icon(共 7 张);card_frame 保留供 T3.5。
- 验收: 重写 TC-22(过滤=Nearest + 真实原生尺寸比例带)+ 新增 TC-22b(全场景 TextureRect 宽高比偏差 <5% 且不越出视口),均通过;TC-23a 改用 4.7 shaped_text API 实测文本不溢出。
- DoD: 全部画面贴图无溢出、无变形;README 已知问题已补记修复结论。
