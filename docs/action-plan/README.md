# Catalyst 行动计划(审查版)

> 版本: v1.0 · 日期: 2026-08 · 状态: **待审查**
> 上游依据: `docs/开发路线图与行动指南.md`(现状分析 + 问题定位),本文件夹是把它拆解为**可逐条执行、可勾选验收**的任务卡。
> 使用方式: 审查通过后逐任务勾选 `[ ]` → `[x]`,每完成一个任务更新其"状态"与对应测试断言。

---

## 0. 目标与原则

1. **先修体验后加内容**:P0/P1 缺陷修复完之前,不做新玩法。
2. **测试先行**:每个任务先更新/新增测试断言,再改实现;两套测试(headless)必须全绿。
3. **数据驱动不动引擎**:新牌/关卡只改 JSON + 枚举(项目既有原则)。
4. **每任务一个 commit**:消息带任务编号(如 `fix(hand): 手牌上限与溢出 T1.2`)。
5. **平衡改动必须重跑基准**:TC-06(第 1 关贪心 20 回合 ≥100)与 TC-16(振荡终止)不得回归。
6. **文档随代码同步**:每阶段收尾更新 README 已知问题、路线图状态、AGENTS.md(如需)。

## 1. 阶段总表

| 阶段 | 目标 | 任务 | 预估工时 | 状态 |
|------|------|------|----------|------|
| 阶段 1 | 稳定性修复(P0) | T1.1 ~ T1.5 | 17h | T1.1 已完成(本次提交),其余待开始 |
| 阶段 2 | 逻辑缺陷与鲁棒性(P1) | T2.1 ~ T2.5 | 10h | 待开始 |
| 阶段 3 | 玩法与体验完善(P2) | T3.1 ~ T3.8 | 26h | 待开始 |
| 阶段 4 | 工程化与内容发布(P3) | T4.1 ~ T4.8 | 28.5h | 待开始 |
| **合计** | | **26 个任务** | **≈81.5h** | |

## 2. 索引

- [阶段 1 —— 稳定性修复](阶段1-稳定性修复.md)(手牌溢出 / 演化跳过 / 三槽存档 / 贴图缩放)
- [阶段 2 —— 逻辑缺陷与鲁棒性](阶段2-逻辑缺陷与鲁棒性.md)(混沌判负 / 选中残留 / 数据降级 / 协程竞态)
- [阶段 3 —— 玩法与体验](阶段3-玩法与体验.md)(FROZEN / 时间戳 / 音效 / 粒子 / 卡面 / 撤销预演 / 种子化)
- [阶段 4 —— 工程化与发布](阶段4-工程化与发布.md)(重构 / 性能 / UI / 第 5 关 / CI 导出)

## 3. 执行约定

- **完成定义(DoD)**:改动点落地 + 对应测试断言更新且 headless 全绿 + commit 含任务编号 + 本文件勾选 + (如影响玩法)README 已知问题同步。
- **测试运行**:
  ```powershell
  # 核心自检: Godot 启动时自动跑,或
  godot --headless --path . --script res://tests/whitebox_tests.gd
  godot --headless --path . --script res://tests/whitebox_tests.gd -- --filter=TC-06
  ```
- **"现状记录"用例的更新纪律**:TC-13 / TC-23b / TC-28b / TC-30a / TC-30b 现在断言的是"有缺陷的行为";对应任务修复后,**必须先改断言再改实现**,防止测试静默变绿。
- **顺序依赖**:阶段 1 → 2 → 3 顺序执行;阶段 4 可与阶段 3 尾部任务并行(标注于任务卡)。

## 4. 验收总清单(测试 ↔ 任务映射)

| 测试用例 | 现状态 | 对应任务 | 修复后应断言 |
|----------|--------|----------|--------------|
| TC-04 / TC-05 | 已修复(工作区) | T1.1 | 文本无 U+FFFD、按钮齐全可聚焦 |
| TC-19 | 已修复(工作区) | T1.1 | 图例含全部 11 元素 |
| TC-13 / TC-13b/c/d(新增) | 现状记录 / 待新增 | T1.4 | 三槽存读一致;覆盖确认与来源槽静默;秒级时间戳 `YYYY-MM-DD HH:MM:SS`;序列化 round-trip 无损 |
| TC-14 / TC-20 | 边界记录 | T1.2 | `hand_size() <= 上限`,布局不溢出 |
| TC-15 | 边界记录 | T1.3 | 增加 speed=∞ 时 <2s 的断言 |
| TC-22 / TC-22b(新增) | 前提错误 / 待新增 | T1.5 | 按真实原生尺寸断言缩放比例;TextureRect 宽高比偏差 <5% 且不越出视口 |
| TC-07 | 异常记录 | T2.1 | `game_ended == true`;水元素有反制手段 |
| TC-28b | 现状记录 | T2.2 | 演化后 `selected_card_idx == -1` |
| TC-23b | 现状记录 | T2.3 | 空文件 → 降级牌组可玩 |
| TC-25 | 现状记录 | T2.4 | 非法 kind 整条跳过不崩溃 |
| TC-30c | 正常 | T2.5 | 扩展断言:切场景后旧演化中止、新局干净 |
| TC-30a | 现状记录 | T3.1 | FROZEN 空格不被 MULTIPLY 填充 |
| TC-30b | 现状记录 | T3.2 | 新格 `placed_at_turn == 当前 turn` |
| 新增(尘放大倍数) | — | T3.8 | 固定种子下尘对连锁的贡献可量化 |

## 5. 变更记录

- v1.0(2026-08):依据开发路线图拆解出 25 个任务卡,提交待审查。
