# 阶段 4 —— 工程化与内容发布(P3)

> 目标:偿还技术债、补内容、打通 CI 与发布管道。
> 完成标准:本阶段 8 个任务全部 DoD,项目可导出发布。
> 说明: T4.5/T4.6 内容任务可与阶段 3 尾部任务并行。

---

## T4.1 世界规则拆分

- 优先级: P3 · 预估: 4h · 状态: [x] 已完成 · 依赖: 无
- 现状: `GameManager._world_rules()` 120 行单体,第 4 关天灾硬编码在里层。
- 改动点: 新建 `src/world/WorldRules.gd` 注册表(或 `data/world_rules.json`),`GameManager.end_turn()` 调 `WorldRules.apply_all(grid, level, rng, turn)`;保持"数据驱动、引擎不动"。
- 验收: 行为逐项等价(对拍旧实现);`run_tests.gd` 世界规则用例全绿。
- 实际落地: 新建 `src/world/WorldRules.gd`,按原顺序拆为蒸汽/草土/生成/燃烧/孢子/雪/天灾/陨石衰减;`GameManager._world_rules` 保留为兼容包装, 传递 grid/level/turn/wind/rng/pillars。

## T4.2 性能优化

- 优先级: P3 · 预估: 4h · 状态: [x] 已完成 · 依赖: 无
- 现状: `_compute_components` 每柱每轮全图 flood-fill;`_snapshot` 每轮拼字符串;`blessed_before` 每 Reaction 重建。
- 改动点: 尘团块组件缓存(尘变化才重算);快照改哈希;BLESSED 集合循环外算一次;16×16 大关 profile 验证。
- 验收: TC-15 同棋盘耗时下降且连锁数不变。
- 实际落地: RuleEngine 尘组件签名缓存(尘布局不变不重算 BFS);ChainReaction 快照字符串改为 `hash()` 整数键;BLESSED 集合每轮反应批次只扫描一次并在祝福反应后增量更新;TC-15/TC-16 回归通过。

## T4.3 UI 资源化与国际化

- 优先级: P3 · 预估: 6h · 状态: [x] 已完成 · 依赖: T3.5(卡面 tscn 化)
- 现状: `LevelSelect` 纯代码建 UI;中文文案散落代码/tscn/卡牌 tooltip。
- 改动点: `LevelSelect.tscn` 化;文案抽 `data/i18n/zh.csv`(或 Godot Translation);卡牌文案由 `rules.json` `desc` 字段驱动。
- 验收: 无硬编码文案遗留(白盒可断言);切语言不破坏布局。
- 实际落地: LevelSelect/SaveSelect 场景增加 Layout 容器;新建 `src/ui/Texts.gd` + `data/i18n/zh.csv`, 两个入口场景全部动态文案改查表;卡牌文案继续由 rules.json desc/tip 驱动;帮助文案由 help.json 驱动;新增 TC-37 校验 CSV 键与入口 UI 文本一致。

## T4.4 清理遗留

- 优先级: P3 · 预估: 0.5h · 状态: [x] 已完成 · 依赖: 无
- 内容: 删除 `data/coast.json` 与 `GameManager.LEVEL_PATH` 未用常量;`tests/whitebox_tests.gd` 补 `.gd.uid`;确认 `.gitignore` 覆盖 `user://` 缓存。
- 验收: 全量测试仍绿;无引用告警。
- 实际落地: 已删 `data/coast.json` 与 `GameManager.LEVEL_PATH`;`tests/whitebox_tests.gd.uid` 已存在;`.gitignore` 已覆盖 `.godot-user/`。

## T4.5 第 5 关与占位机制启用

- 优先级: P3 · 预估: 6h · 状态: [x] 已完成 · 依赖: T3.8(种子化)
- 现状: `STEAMED`/`ASH` 状态、`trigger_state`/`contact_coord` 字段、FROZEN 加冻均未使用——引擎已预留扩展位。
- 改动点(数据驱动,基本不动引擎):
  1. 第 5 关(18×18"深渊",目标建议 3000):用 `trigger_state` 做状态触发牌,启用 STEAMED/ASH。
  2. FROZEN 加冻:冰邻水 → 水加 FROZEN(新世界规则,按 T4.1 注册表加入)。
  3. 新天灾:酸雨(清状态)/ 磁暴(随机移动柱子)。
- 验收: 新牌新关卡只改 JSON + 枚举,引擎零改动;两套测试回归 + 新关可用 TC-06 式贪心脚本验证可通关。
- 实际落地: 第5关 `data/levels/abyss.json`(18×18, 目标3000);新牌 过热蒸汽(STEAMED)/灰烬转化(ASH)/深渊净化(level=5);WorldRules 增加冰邻水冻结与第5关酸雨/磁暴;GridRenderer 绘制 STEAMED/ASH;新增 TC-38 贪心5回合内通关。

## T4.6 关卡目标多样性(策划 3.2)

- 优先级: P3 · 预估: 4h · 状态: [ ] 待开始 · 依赖: 无
- 内容: 关卡 JSON 加 `goals` 数组:累计目标 / 单次演化峰值 / 限定回合 / 禁用元素;通关界面逐项打勾 + 1-3 星评价。
- 验收: 每项目标有对应断言;TC-06 作为基础难度基准保留。

## T4.7 CI 与发布

- 优先级: P3 · 预估: 4h · 状态: [ ] 待开始 · 依赖: 无
- 内容: GitHub Actions 拉 Godot headless(如 barichello/godot-ci),跑两套测试,全绿才允许合入;导出预设(Windows/Linux/Web)+ 图标 + itch.io 发布脚本。
- 验收: PR 自动跑测试;导出包可运行;README 补发布说明。

## T4.8 文档同步机制(流程项)

- 优先级: P3 · 预估: 每阶段 0.5h · 状态: [ ] 持续
- 内容: 每阶段收尾核对并更新:AGENTS.md(阶段/参数)、README(规则表/已知问题)、`docs/开发路线图与行动指南.md`(状态)、本计划(勾选)。
- 验收: 交付物与代码一致(以白盒断言为准)。
