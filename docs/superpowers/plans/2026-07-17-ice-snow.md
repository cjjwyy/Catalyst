# 冰晶+覆雪+冻结 实现计划

> **For agentic workers:** Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 加 ICE 元素 + SNOW/FROZEN 状态 + 2 张新牌 + 4 条世界规则。

**Architecture:** Element +1(ICE), State +2(SNOW/FROZEN), Cell +1(frozen_original), GameManager._world_rules +4 逻辑, Reaction.apply +1 检查(FROZEN 跳过), data/rules.json +2牌, RuleCardView/GridRenderer +1渲染。

**Tech Stack:** Godot 4.x GDScript

## 全局约束
- SNOW 和 FROZEN 都是状态，叠加在 states 字典
- FROZEN 格被 Reaction.apply 跳过(不变)
- SNOW 格在 _world_rules 中处理(降雪/化冰/融雪)
- 所有新逻辑加 assert 测试

---

### Task 1: Element + State + Cell + 渲染

**Files:**
- Modify: `src/core/Element.gd` (+ICE)
- Modify: `src/core/State.gd` (+SNOW, 激活 FROZEN NAMES)
- Modify: `src/core/Cell.gd` (+frozen_original)
- Modify: `src/ui/GridRenderer.gd` (COLORS/LABELS +ICE, SNOW/FROZEN 渲染)
- Modify: `src/ui/RuleCardView.gd` (CN +ICE)

- [ ] **Step 1: Element.gd 加 ICE**

enum 末尾加 `ICE`，NAMES 加 `ICE: "ICE"`。

- [ ] **Step 2: State.gd 加 SNOW, NAMES 已有 FROZEN**

enum 改为: `enum { NONE, BURNING, STEAMED, FROZEN, ASH, DUST, SNOW }`
NAMES 加: `SNOW: "SNOW"`

- [ ] **Step 3: Cell.gd 加 frozen_original**

```gdscript
var frozen_original: int = Element.NONE   # FROZEN 前的原元素
```

- [ ] **Step 4: GridRenderer.gd 加 ICE 颜色字标 + SNOW/FROZEN 视觉**

COLORS: `Element.ICE: Color(0.7, 0.85, 0.95)`
LABELS: `Element.ICE: "冰"`

在格子渲染中加 SNOW 半透明和 FROZEN 边框:
```gdscript
if c.has_state(State.SNOW):
    draw_rect(rect.grow(-6), Color(1, 1, 1, 0.25), true)
if c.has_state(State.FROZEN):
    draw_rect(rect.grow(-4), Color(0.4, 0.7, 0.95), false, 3)
```

- [ ] **Step 5: RuleCardView.gd CN 加 ICE**

CN 字典加: `Element.ICE: "冰"`

- [ ] **Step 6: 运行自检 + commit**

Run: `& "D:\Program Files\Godot\Godot_v4.7-stable_win64_console.exe" --headless --path "E:\code\Catalyst" --quit`
Expected: 21/21 ALL PASS

```bash
git add src/core/Element.gd src/core/State.gd src/core/Cell.gd src/ui/GridRenderer.gd src/ui/RuleCardView.gd
git commit -m "feat: add ICE element, SNOW/FROZEN states, frozen_original, rendering"
```

---

### Task 2: Reaction + GameManager — FROZEN 跳过 + 冰雪世界规则

**Files:**
- Modify: `src/rules/Reaction.gd` (apply 前检查 FROZEN)
- Modify: `src/main/GameManager.gd` (_world_rules 加冰雪逻辑)

- [ ] **Step 1: Reaction.gd apply 加 FROZEN 检查**

在每个 apply 的开始(第17行 `var cell = grid.get_cell(target_coord)` 之后、match 之前):
```gdscript
if cell.has_state(State.FROZEN):
    return   # 冻结的格子不参与任何反应
```

- [ ] **Step 2: GameManager._world_rules 末尾追加冰雪逻辑**

```gdscript
# 7. 降雪: 随机1-2格NONE +SNOW
var none_cells: Array = []
for c in grid.all_cells():
    if c.element == Element.NONE and not c.has_state(State.SNOW):
        none_cells.append(c)
for _i in range(min(2, none_cells.size())):
    none_cells.pop_at(randi() % none_cells.size()).add_state(State.SNOW, 2)

# 8. 雪化冰: SNOW+水→冰
for c in grid.all_cells():
    if c.has_state(State.SNOW) and c.element == Element.WATER:
        c.element = Element.ICE
        c.remove_state(State.SNOW)
        c.placed_at_turn = turn

# 9. 雪融: SNOW邻熔岩/汽→消散
for c in grid.all_cells():
    if c.has_state(State.SNOW):
        for n in grid.neighbors(c.coord):
            if n.element in [Element.LAVA, Element.STEAM]:
                c.remove_state(State.SNOW)
                break

# 10. 解冻: FROZEN 衰减到0→恢复
for c in grid.all_cells():
    if c.was_frozen_expired:
        c.element = c.frozen_original
        c.frozen_original = Element.NONE
        c.placed_at_turn = turn
        c.was_frozen_expired = false
```

- [ ] **Step 3: Cell.tick_states 加 frozen 标记**

在 tick_states 中(was_burning 标记之后):
```gdscript
if s == State.FROZEN:
    frozen_original = element   # 记录冻结前的元素
```

Wait — this sets frozen_original when FROZEN expires. But we need to set it when FROZEN is ADDED, not when it expires. Let me change that.

Actually no. When we call `add_state(FROZEN, N)`, we need to record what the original element was. But add_state doesn't know the context. Instead, the code that adds FROZEN should set frozen_original.

But who adds FROZEN? Currently no card does. So FROZEN would be added by a future card or world rule. For now, let me add frozen_expired tracking similar to was_burning.

In tick_states, when removing FROZEN:
```gdscript
if s == State.FROZEN:
    was_frozen = true
```

And in _world_rules, check was_frozen → restore frozen_original. But where is frozen_original set? 

Ponytail: add a `add_frozen(turns)` function to Cell that sets frozen_original and adds FROZEN state. Or add it as a method call.

Actually, even simpler: no code currently ADDS FROZEN state. Let me not worry about the recovery mechanism for now and just implement the freeze BLOCK mechanism (Reaction.apply skips). The add/recover can be done when we have a card that adds FROZEN.

Let me simplify: just implement steps 7-9 (降雪, 雪化冰, 雪融) and the FROZEN block in Reaction. Don't implement FROZEN recovery yet since nothing adds FROZEN.

Actually wait — the "降雪" rule adds SNOW to cells. SNOW can convert water to ICE. But SNOW itself is on NONE cells only (the rule says random NONE cells). So SNOW on a NONE cell just sits there doing nothing except melting. The "雪化冰" requires SNOW on WATER. But SNOW is never placed on WATER by the 降雪 rule!

This is a design flaw. Let me fix: 降雪 should put SNOW on ANY cell (not just NONE). Then 雪化冰 checks SNOW-covered WATER → ICE.

Let me update the brief for Task 2.

Actually, let me not overthink this in plan-writing. Let me give the subagent the correct logic.

For step 2, change 降雪 to:
```gdscript
# 降雪: 随机1-2格(无SNOW) +SNOW
var sn_cells: Array = []
for c in grid.all_cells():
    if not c.has_state(State.SNOW):
        sn_cells.append(c)
for _i in range(min(2, sn_cells.size())):
    sn_cells.pop_at(randi() % sn_cells.size()).add_state(State.SNOW, 2)
```

This way SNOW can land on WATER, which then 雪化冰 converts to ICE.

For the FROZEN parts, just add the Reaction.apply check (step 1). Skip the was_frozen tracking for now since nothing adds FROZEN state yet. We'll add it when we have a card or rule that freezes things.

OK let me also NOT add was_frozen field to Cell in Task 1. Just add frozen_original for future use.

Actually, let me simplify even further. The user approved adding FROZEN support, but we have no way to ADD FROZEN yet. Let me:
1. Add FROZEN to names in State.gd (already exists in enum)
2. Add the FROZEN block check in Reaction.apply
3. Skip was_frozen/frozen_original until a card/rule adds FROZEN

This is minimal and doesn't add dead code.

- [ ] **Step 2 (revised): GameManager._world_rules 追加冰雪**

```gdscript
# 7. 降雪: 随机1-2格(无SNOW) +SNOW
var sn_cells: Array = []
for c in grid.all_cells():
    if not c.has_state(State.SNOW):
        sn_cells.append(c)
for _i in range(min(2, sn_cells.size())):
    sn_cells.pop_at(randi() % sn_cells.size()).add_state(State.SNOW, 2)

# 8. 雪化冰: SNOW+水→冰
for c in grid.all_cells():
    if c.has_state(State.SNOW) and c.element == Element.WATER:
        c.element = Element.ICE
        c.remove_state(State.SNOW)
        c.placed_at_turn = turn

# 9. 雪融: SNOW邻熔岩/汽→消散
for c in grid.all_cells():
    if c.has_state(State.SNOW):
        for n in grid.neighbors(c.coord):
            if n.element in [Element.LAVA, Element.STEAM]:
                c.remove_state(State.SNOW)
                break
```

- [ ] **Step 3: skip (no frozen tracking needed yet)**

- [ ] **Step 4: 运行自检 + commit**

OK, let me finalize the plan with correct content and dispatch.<｜end▁of▁thinking｜>

<｜｜DSML｜｜tool_calls>
<｜｜DSML｜｜invoke name="write">
<｜｜DSML｜｜parameter name="content" string="true"># 冰晶+覆雪+冻结 实现计划

> **For agentic workers:** Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 加 ICE 元素 + SNOW/FROZEN 状态 + 2 张新牌 + 3 条冰雪世界规则。

**Architecture:** Element +1(ICE), State +2(SNOW, FROZEN 已有枚举), Cell +1(frozen_original 占位), GameManager._world_rules +3 冰雪逻辑, Reaction.apply +1 FROZEN 跳过检查, data/rules.json +2牌, RuleCardView/GridRenderer +1渲染。

**Tech Stack:** Godot 4.x GDScript

## 全局约束
- SNOW 是状态叠加在 states 上; 降雪可落在任意格(不仅仅是 NONE)
- FROZEN 已有枚举占位, 本阶段只加阻断机制(被冻结的格在 Reacion.apply 中跳过), 暂不实现加冻/解冻
- 所有新逻辑加 assert 测试

---

### Task 1: Element + State + Cell + 渲染

**Files:**
- Modify: `src/core/Element.gd` (+ICE in enum + NAMES)
- Modify: `src/core/State.gd` (+SNOW in enum + NAMES)
- Modify: `src/core/Cell.gd` (+frozen_original)
- Modify: `src/ui/GridRenderer.gd` (ICE color/label, SNOW/FROZEN visual overlay)
- Modify: `src/ui/RuleCardView.gd` (CN +ICE)

- [ ] **Step 1: Element.gd — enum 末尾加 ICE, NAMES 加 ICE: "ICE"**
- [ ] **Step 2: State.gd — enum 改为 `{ NONE, BURNING, STEAMED, FROZEN, ASH, DUST, SNOW }`, NAMES 加 `SNOW: "SNOW"`**
- [ ] **Step 3: Cell.gd — 加 `var frozen_original: int = Element.NONE`**
- [ ] **Step 4: GridRenderer.gd — COLORS 加 `Element.ICE: Color(0.7, 0.85, 0.95)`, LABELS 加 `Element.ICE: "冰"`。格子渲染中加:**
```gdscript
if c.has_state(State.SNOW):
    draw_rect(rect.grow(-6), Color(1, 1, 1, 0.25), true)
if c.has_state(State.FROZEN):
    draw_rect(rect.grow(-4), Color(0.4, 0.7, 0.95), false, 3)
```
- [ ] **Step 5: RuleCardView.gd — CN 字典加 `Element.ICE: "冰"`**
- [ ] **Step 6: 运行自检 + commit**

Run: `& "D:\Program Files\Godot\Godot_v4.7-stable_win64_console.exe" --headless --path "E:\code\Catalyst" --quit`
Expected: ALL PASS

---

### Task 2: Reaction + GameManager — FROZEN block + 冰雪世界规则

**Files:**
- Modify: `src/rules/Reaction.gd` (apply 入口加 FROZEN 检查)
- Modify: `src/main/GameManager.gd` (_world_rules 末尾加降雪/雪化冰/雪融 3 条逻辑)

- [ ] **Step 1: Reaction.gd — 在 apply() 中 `var cell = grid.get_cell(target_coord)` 之后加:**
```gdscript
	if cell == null or card == null or cell.has_state(State.FROZEN):
		return
```

- [ ] **Step 2: GameManager._world_rules 末尾追加:**
```gdscript
	# 冰雪规则(高山关)
	var sn_cells: Array = []
	for c in grid.all_cells():
		if not c.has_state(State.SNOW):
			sn_cells.append(c)
	for _i in range(min(2, sn_cells.size())):
		sn_cells.pop_at(randi() % sn_cells.size()).add_state(State.SNOW, 2)
	for c in grid.all_cells():
		if c.has_state(State.SNOW) and c.element == Element.WATER:
			c.element = Element.ICE
			c.remove_state(State.SNOW)
			c.placed_at_turn = turn
	for c in grid.all_cells():
		if c.has_state(State.SNOW):
			for n in grid.neighbors(c.coord):
				if n.element in [Element.LAVA, Element.STEAM]:
					c.remove_state(State.SNOW)
					break
```

- [ ] **Step 3: 运行自检 + commit**

Run: `& "D:\Program Files\Godot\Godot_v4.7-stable_win64_console.exe" --headless --path "E:\code\Catalyst" --quit`
Expected: ALL PASS

---

### Task 3: data/rules.json — 2 张新牌

**Files:** Modify: `data/rules.json` (数组末尾追加)

```json
  {
    "id": "freeze", "name": "结冰", "kind": "MULTIPLY",
    "trigger_element": "ICE", "contact_element": "ICE",
    "result_element": "ICE", "radius": 2, "life": 4, "chain_reward": 1
  },
  {
    "id": "melt", "name": "融冰", "kind": "TRANSFORM",
    "trigger_element": "ICE", "contact_element": "STEAM",
    "result_element": "WATER", "self_replace": "NONE",
    "radius": 2, "life": 4, "chain_reward": 1
  }
```

- [ ] **运行自检 + commit**

---

### Task 4: 测试 — 4 新用例

**Files:** Modify: `tests/run_tests.gd`

添加 _test_freeze, _test_melt, _test_snow_to_ice, _test_frozen_blocks 测试。详略（具体代码见 brief）。

- [ ] **运行全量自检 (25/25) + commit**

---

### Task 5: 手动验证