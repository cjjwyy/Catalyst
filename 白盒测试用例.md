# Catalyst 白盒测试用例表

> 版本: v2.0(UTF-8 重建)· 与 `tests/whitebox_tests.gd` 一一对应,共 **47 条**。
> 来源: 功能点/可测试点风险清单(R01-R20,原始清单已不在仓库中,本表省略 R 编号)。
> 约定: 所有用例可在 Godot 4.x headless 下以 GDScript 脚本执行(参照 `tests/run_tests.gd` 的 assert 模式);涉及文件注入/场景切换的用例使用测试夹具临时副本,不修改仓库内真实文件(TC-23b 会备份并恢复 `rules.json`)。每条用例只验证一件事;预期结果均为可编程断言,不含主观观感。

## 运行

```powershell
godot --headless --path <项目根目录> --script res://tests/whitebox_tests.gd
# 只跑某个用例:
godot --headless --path <项目根目录> --script res://tests/whitebox_tests.gd -- --filter=TC-06
```

## 编号说明

人工审查版清单存在重复编号(TC-23/TC-26/TC-28 各 2 条,TC-30 共 3 条),脚本以 a/b/c 后缀去重,与本表一一对应(TC-23a=帮助文本高度,TC-23b=空规则文件降级,以此类推)。

## 用例类型约定

- **正常**:当前应通过。
- **边界**:量化当前行为的边界基准(性能/尺寸/容量)。
- **已知缺陷(预期失败)**:当前必然失败,用于回归验证修复(TC-04 帮助文本乱码、TC-19 图例缺孢/冰)。
- **现状记录**:阶段3完成后当前已无现状记录用例(历史记录用例 TC-30a/30b 已修复转正常)。

## 用例表

| 编号 | 名称 | 类型 | 前置条件 | 操作步骤 | 预期结果(断言) |
|------|------|------|----------|----------|----------------|
| TC-01 | 关卡选择页可打开 | 正常 | Godot 从 `project.godot` 启动,GameManager autoload 已加载 | 加载 `res://scenes/LevelSelect.tscn`,统计 `buttons` 数组;另加载 SaveSelect 入口场景 | 场景无报错;`buttons.size() == 4`;`buttons[i].disabled == (i > unlocked)`(锁定随持久化进度);SaveSelect 含 BG 节点 |
| TC-02 | 四关开局状态 | 正常 | 场景树中存在 /root/GameManager | 依次执行 `GameManager.start_game(0..3)`,检查 `grid.w/grid.h` 与 `target` | 4 关分别为 10×10/100、12×12/300、14×14/700、16×16/1500;`hand_size() == 5`;`energy.current == 3`;`chain_total == 0` |
| TC-03 | 场景往返导航后状态干净 | 正常 | 进入第 1 关后切回关卡选择再进 Main | 实例化 LevelSelect → Main → `start_game(0)` | `pillars.is_empty()`;`chain_total == 0`;`game_ended == false`;`phase == LAYOUT`;`dead_turns == 0` |
| TC-04 | 帮助面板文本无乱码 | 正常 | `Main.tscn` 已实例化 | 读取 `HelpPanel/HelpLabel.text`,断言不含 U+FFFD 且含关键词 目标/演化/右键/撤回 | 文本无 U+FFFD 且含 目标/演化/右键/撤回 关键词(阶段1已修复) |
| TC-05 | 主要按钮键盘可聚焦 | 正常 | `Main.tscn` 已实例化 | 枚举 HelpButton/ExecuteButton/MenuButton/NextButton/RetryButton | 每个按钮 `focus_mode != Control.FOCUS_NONE`(Tab 可到达);GameOverPanel 存在 |
| TC-06 | 第 1 关贪心 20 回合可通关 | 正常 | `start_game(0)` 完成 | 固定种子,贪心脚本每回合优先打 steamify/grow/harvest,直到能量耗尽后 `execute()`,重复 20 回合 | 20 回合内 `chain_total >= 100`(难度回归基准,任何平衡改动后必须重跑) |
| TC-07 | 混沌失控即死——水元素 | 正常 | 10×10 网格 51 格为 WATER(>50%),场上无 trigger==WATER 的 EXTINCTION 柱 | 执行 `chaos_check()`,监听 `game_over` 信号 | 收到 `game_over(false, "混沌失控 — 水 超过 50%")`;`game_ended == true`、`phase == EVOLVE`;牌池含「干涸」反制牌(P1-4 已修复) |
| TC-08 | 混沌时灭绝柱可化解 | 正常 | 10×10 网格 51 格 PLANT,存在 extinct 柱(radius=2) | 执行 `chaos_check()` | 不触发判负;柱半径 2 内 PLANT 被清空;`game_ended == false` |
| TC-09 | 世界规则每回合生成量 | 边界 | `start_game(0)` 完成 | 连续 `end_turn()` 10 次不落柱,统计 WATER+STONE 净增 | 10 回合净增 20±4(每回合 2,容差 ±4);所有新生成格 `placed_at_turn == 当前 turn` |
| TC-10 | 第 4 关天灾概率与陨石化岩 | 正常 | `start_game(3)`(第 4 关) | 固定种子循环 `_world_rules()` 100 次,统计陨石/地震/火山触发;另测 METEOR_LAVA 衰减 | 100 回合触发 30±10 次(30% 概率);陨石格 2 次 `tick_states()` 后 LAVA→STONE(`was_meteor` 机制) |
| TC-11 | 死寂 10 回合判负 | 边界 | `start_game(0)` 完成 | 连续 10 次 `execute()`(不落柱,0 连锁),监听 `game_over` | 第 10 次后收到 `game_over(false, "失败: 世界进入死寂")`;`game_ended == true` |
| TC-12 | BLESSED 格连锁翻倍 | 正常 | 网格 (1,1)=WATER+BLESSED,(2,1)=LAVA;steamify 柱 | `ChainReaction.execute(grid, [pillar])` | `chain == 2`(chain_reward 1 ×2,`ChainReaction.gd` BLESSED 检查) |
| TC-13 | 三槽存档读写 | 正常 | 测试专用存档路径(隔离真实存档) | 存槽 → 新 SaveManager 实例模拟重启 → `load_game(0)` 恢复 | 槽名保留;读档后关卡/turn/chain 恢复;网格哈希与存档一致;删槽后全局 unlocked 仍保留(独立持久化) |
| TC-13b | 覆盖确认与来源槽 | 正常 | 测试专用存档路径 | 存槽 → load_slot → 再存同槽/异槽 → 删除来源槽 | 无来源槽存非空槽需确认;来源槽静默覆盖;覆盖后标记更新;删除来源槽后标记置 -1 |
| TC-13c | 时间戳与增删改 | 正常 | 测试专用存档路径 | 存档后校验时间文本;重命名;删除 | 时间文本匹配 `^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}$`(秒级);重命名/删除生效;空槽时间文本为空 |
| TC-13d | 序列化往返无损 | 正常 | 测试专用存档路径 | 造状态(元素/尘/柱/手牌/回合/风)→ 存档 → 破坏现场 → 读档 | round-trip 后网格哈希/手牌数/柱数/回合/风一致 |
| TC-14 | 手牌上限与布局容量 | 边界 | `start_game(0)` 完成 | 连续 30 次 `end_turn()` 不打牌,断言手牌数、牌守恒与所需宽度 | `hand_size() == 8`(上限,HandManager.MAX_HAND);手牌+抽牌堆+弃牌堆 == 第 1 关牌池 12(守恒);8×130+7×6 = 1082 ≤ 1100px(不再溢出) |
| TC-15 | 200 连锁演化耗时与跳过 | 边界 | 40×20 棋盘格 WATER/LAVA + 8 根 r=5 steamify 柱 | `execute_async` 三档计时:1x / speed≤0(跳过)/ 4x;同种子比对结果 | 1x chain ≈ 200±20、耗时 (18s, 45s];跳过档 <2s 且连锁与终局网格同同步执行一致;4x <12s 且快于 1x、结果一致 |
| TC-16 | 振荡棋盘有限终止 | 正常 | grow+extinct 振荡棋盘(参照 `_test_extinct_terminates`) | `ChainReaction.execute(grid, pillars)` | 正常返回,`chain < 1000`(cycle 快照去重截断,不进入死循环) |
| TC-17 | 各关格子尺寸与可视范围 | 正常 | `GridRenderer` 实例 | 4 关分别 `set_grid(grid)`,读取 cell_size 与网格外接矩形 | cell_size == 64/64/60/52(10/12/14/16 关);网格矩形完全落在 1500×1000 视口内(第 4 关右缘 1012、下缘 872) |
| TC-18 | 16×16 关控件互不遮挡 | 边界 | 第 4 关 `set_grid` | 计算网格右下角与图例 origin,比对 ExecuteButton/MenuButton 区域 | 网格与图例不与按钮相交;所有可点击控件互不遮挡 |
| TC-19 | 图例覆盖全部元素 | 正常 | `GridRenderer` 已加载 | 枚举 `_draw_legend()` 的 items 与 `Element` 枚举求差集 | 图例含全部 11 元素(阶段1已修复) |
| TC-20 | 手牌区宽度容量 | 边界 | `start_game(0)` / `start_game(3)` | 计算各关牌池数与所需宽度 | 第 1 关牌池 12(8 基础含干涸 + 2×steamify + 2×grow),8 张上限宽 1082 ≤ 1100 可容纳;第 4 关牌池 14,若全展开 14×130+13×6 > 1100 —— 手牌上限为必要约束 |
| TC-21 | 元素与特效贴图资源完整 | 正常 | 项目资源可访问 | 遍历 ELEMENT_PATHS/OVERLAY_PATHS + pillar/dust 路径逐一 `load()` | 每个资源非 null 且尺寸 >0(11 元素 + 4 overlay + 2 特效图全部存在) |
| TC-22 | 贴图缩放比例区间 | 边界 | 16×16 关,cell_size == 52 | 校验项目 default_texture_filter;对全部元素贴图计算 绘制/原生 比例 | 项目过滤 == 0(Nearest);每张元素贴图比例 ∈ [1/64, 1/4](原生 1024/1254px → 绘制 52px) |
| TC-22b | 贴图矩形宽高比与越界 | 正常 | LevelSelect/Main 场景可实例化 | 枚举场景内全部 TextureRect,比对 rect 宽高比与贴图宽高比 | 每个节点宽高比偏差 <5%,且 rect 完整落在 1500×1000 视口内 |
| TC-23a | 帮助文本不溢出面板 | 正常 | `Main.tscn` 已实例化 | 用 TextServer 测量 HelpLabel 文本(684px 宽、12px 字号)渲染高度 | 文本高度 ≤ 650px(HelpPanel 高度),不溢出面板 |
| TC-23b | 空规则文件降级 | 正常 | 可读写 `data/rules.json`(用例会备份恢复) | 将 rules.json 写为 `[]`/损坏 JSON 后加载/开局,另注入非法关卡 JSON,断言后恢复文件 | 空文件 → 内置 DEFAULT_RULES 3 张、手牌 5、可出牌;损坏文件 → `game_over` 含「规则数据损坏」且 `game_ended == true`;关卡 size/target 钳制为 6-32 / 1-99999,非法元素条目跳过;恢复文件后手牌 5 |
| TC-25 | 非法字段静默降级 | 正常 | `RuleCard.from_dict` 可调用,`rules.json` 可写(用例会备份恢复) | 非法元素名 + 合法字段;非法 kind + 合法 radius;越界 radius/life/threshold;注入含非法 kind 的 rules 数组 | 非法元素名 → 降级 NONE,合法字段保留;非法 kind → `from_dict` 返回 false、字段保持默认、整条跳过不崩溃;radius/life/threshold 钳制到安全带;文件加载只保留合法条目 |
| TC-26a | 界外点击与越界坐标 | 正常 | `start_game(0)` 完成 | `world_to_coord` 负坐标/超界坐标;`play_card(-1/界外坐标/合法坐标)` | 界外返回 (-1,-1);非法落柱返回 false;合法落柱 true;网格状态不变 |
| TC-26b | 半径扫描边界格数 | 正常 | 6×6 网格 | `cells_in_radius((0,0), 2)` 统计格数与坐标 | 角点半径 2 共 6 格(曼哈顿 ≤2 且界内),坐标为 (0,0)(0,1)(0,2)(1,0)(1,1)(2,0) |
| TC-28a | 催化剂尘越界消失 | 正常 | `start_game(0)`,wind_dir=0(北),speed=3 | 给 (0,0) 加 DUST(1 回合),执行 `push_dust()` | 尘格被移除,全网格残留 DUST 0 格(越界消失) |
| TC-28b | 演化后清空选中 | 正常 | `start_game(0)`,落柱并演化,Main 场景实例化 | `select_card(0)` → `play_card` → 通过 Main 执行按钮启动演化 | 点击执行后立即 `selected_card_idx == -1`,演化结束后仍为 -1(误点网格不会误打旧索引) |
| TC-30a | FROZEN 阻断作用范围 | 正常 | FROZEN 目标格 / FROZEN 空格作扩散目标 | 两组 `ChainReaction.execute()` | ① FROZEN 目标格不被转化、状态保留;② 现状:FROZEN 空格仍被 MULTIPLY 填充(FROZEN 仅挡 trigger 不挡扩散目标,语义统一后更新断言) |
| TC-30b | MULTIPLY 时间戳语义 | 正常 | (1,1)=PLANT,(2,1)=STEAM,grow 柱 | `execute()` 后检查新格 `placed_at_turn` | 新植格 `placed_at_turn == 1`(触发格 0 + 1)—— 现状:非当前 turn,语义偏差,修复后更新断言 |
| TC-30c | 演化中切换场景健壮性 | 正常 | `start_game(0)`,铺 WATER/LAVA 棋盘,Main 场景实例化 | 启动 `execute()` 不等待,0.3s 后切回 LevelSelect 并 `start_game(0)`,再等 1.5s | 启动后有未取消 runner;`start_game` 置旧 runner `cancelled == true` 并清空 `_running_runner`;新局 chain/pillars/dead 干净;旧协程退出后不得污染新局 |
| TC-31a | 卡牌说明文案完整 | 正常 | `rules.json` 可读 | 遍历 17 张牌并 `_load_rules()` | 每张 `desc/tip` 非空、≤80字、无 U+FFFD;id 唯一;RuleCard 解析全部字段 |
| TC-31b | 帮助手册6页易读 | 正常 | Main 场景实例化,`data/help.json` 存在 | 逐页刷新并 shaped-text 测量 | 6页均可翻页;标题/摘要/示例齐全;每页文本不溢出面板 |
| TC-31c | 首次引导不污染状态 | 正常 | FORCE_ONBOARDING=1,`onboarding_seen=false` | Main 实例化 → 连续下一步 → 跳过 | 5步引导可推进;跳过后面板关闭、meta 持久化;网格/手牌/能量/回合不变 |
| TC-32 | 卡面场景与手动保留 | 正常 | `start_game(0)` + RuleCardView.tscn | 手动保留模式抽牌→点击弃牌;实例化卡面 | 进入 RETAIN;弃到3张回 LAYOUT;卡框/色带/标题/效果文本齐全 |
| TC-33 | 回合撤销快照 | 正常 | `start_game(0)` 并落柱 | 连续3轮 end_turn→undo_turn | 每轮撤销后网格哈希/柱/手牌/能量/回合与快照一致;栈耗尽不可撤销 |
| TC-34 | 预演不改真实状态 | 正常 | 铺 WATER/LAVA + steamify 柱 | `preview_evolution()` → 再真实执行 | 预演返回连锁>0且受影响格非空;能量-1;真实网格/连锁不变;真实执行与预演连锁一致 |
| TC-35 | 同种子回放一致 | 正常 | `start_game_with_seed(0, 424242)` | 开局+3回合状态指纹,重置同种子重放 | 两局逐回合指纹(网格/手牌/牌堆/风/回合/能量)完全一致 |
| TC-36 | 尘对连锁放大可量化 | 正常 | 同布局无尘桥/有尘桥 | 同种子 `ChainReaction.execute` | 无尘桥连锁0;有尘桥连锁≥1;放大贡献≥1 |
| TC-37 | 入口文案i18n化 | 正常 | LevelSelect/SaveSelect + `data/i18n/zh.csv` | 实例化两场景并对照 Texts 键值 | 标题/按钮/placeholder/每日挑战等动态文案来自 CSV;Layout 容器存在 |
| TC-38 | 第5关新机制与可通关 | 正常 | `start_game(4)` | 检查牌池;superheat→ashify;FROZEN加冻;酸雨/磁暴;贪心30回合 | 新牌可用、状态触发链>0、两种天灾均出现、`chain_total ≥ 3000` |
| TC-39 | 关卡目标与星级 | 正常 | `start_game(0)` | 设 chain/peak/turn 后 `evaluate_goals`;注入 turn limit 后 `end_turn` | goals=3 且 stars=3;超限定回合判负并提示 |

## 修复追踪(与 docs/开发路线图与行动指南.md 联动)

| 用例 | 缺陷 | 路线图条目 | 修复后动作 |
|------|------|------------|------------|
| TC-04 / TC-05 | Main.tscn 编码损坏 | T1.1 | ✅ 已修复,用例转正常 |
| TC-19 | 图例缺孢/冰 | T1.1 | ✅ 已修复,用例转正常 |
| TC-13 | 进度不持久化 | P0-3 / T1.4 | 重写为三槽存档断言:存读一致、unlocked 独立持久化;新增 TC-13b/c/d(覆盖确认、秒级时间戳、序列化 round-trip) |
| TC-14 / TC-20 | 手牌无上限/溢出 | P0-1 | 断言改为 hand_size ≤ 上限 |
| TC-15 | 演化无跳过 | P0-2 | 增加 speed=∞ 耗时断言 |
| TC-22 | 断言前提错误(原图实为 1024/1254px) | P0-4 / T1.5 | 断言改为按真实原生尺寸;新增 TC-22b 校验 TextureRect 宽高比与越界 |
| TC-07 | 混沌 game_ended 不置位 | P1-4 / T2.1 | ✅ 已修复:game_ended/phase 置位 + 干涸反制 |
| TC-23b | 空规则文件死锁 | P1-6 / T2.3 | ✅ 已修复:空文件降级可玩,损坏文件明确失败 |
| TC-25 | 非法 kind 崩溃 | P1-7 / T2.4 | ✅ 已修复:整条跳过不崩溃 + 数值钳制 |
| TC-28b | 演化后选中残留 | P1-5 / T2.2 | ✅ 已修复:selected_card_idx == -1 |
| TC-30a | FROZEN 语义 | P2-9 / T3.1 | ✅ 已修复:FROZEN 空格不被填充 |
| TC-30b | MULTIPLY 时间戳 | P2-10 / T3.2 | ✅ 已修复:placed_at_turn == 当前 turn |
| TC-31a/b/c | 新人引导与文案 | T3.9 | ✅ 已实现:desc/tip、6页帮助、5步引导 |
| TC-32 | 卡面/保留 | T3.5 | ✅ 已实现:场景卡面 + 手动保留3张 |
| TC-33 | 撤销 | T3.6 | ✅ 已实现:连续3轮回滚 |
| TC-34 | 预演 | T3.7 | ✅ 已实现:副本预演不污染真实状态 |
| TC-35/36 | 种子/尘放大 | T3.8 | ✅ 已实现:同种子回放一致 + 尘贡献量化 |
